    const numeros = [100, 200, 300, 400, 500, 600];
//       variável      limite        incremento
//                  0       6           +1, poderia ser escrito i=i+1
    for (let i = 0; i < numeros.length; i++){
        console.log (`Posição: ${i}. Valor: ${numeros[i]}`);
    };